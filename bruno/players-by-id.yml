info:
  name: players-by-id
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/players/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
