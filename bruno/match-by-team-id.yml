info:
  name: match-by-team-id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/match/team/1601"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
