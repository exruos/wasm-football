info:
  name: teams-by-api-id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/teams/api-id/9930"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
