info:
  name: players-id-not-found
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/players/999999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
