info:
  name: players-invalid-id
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/players/invalid-id-string"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
