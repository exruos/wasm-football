info:
  name: players-no-id
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/players/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
