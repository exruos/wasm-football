info:
  name: match-by-id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/match/145"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
