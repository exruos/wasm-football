info:
  name: not-found-route
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/does-not-exist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
