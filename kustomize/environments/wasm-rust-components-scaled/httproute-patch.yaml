apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: football-app-route
  namespace: football
spec:
  rules:
    - backendRefs:
        - name: keda-add-ons-http-interceptor-proxy
          namespace: keda
          port: 8080
