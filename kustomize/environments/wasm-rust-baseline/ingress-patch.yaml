apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: football-app
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: football-app
                port:
                  number: 80