apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../wasm-rust-baseline

components:
  - ../../components/keda

patches:
  - target:
      kind: SpinApp
      name: football-app
    patch: |-
      - op: add
        path: /spec/enableAutoscaling
        value: true