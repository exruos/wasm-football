[project]
name = "spin-py"
version = "0.1.0"
description = "Python implementation of the football API"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "componentize-py==0.23.0",
    "spin-sdk==4.0.0",
]
